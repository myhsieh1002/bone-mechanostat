# Results never live in the synced tree -- see getResultsDir.m
results/
*.mat

# macOS / iCloud
.DS_Store
.icloud
*.icloud

# MATLAB
*.asv
*.m~
slprj/
codegen/

# Copyrighted reference PDFs — kept local, never redistributed.
# The version-controlled artifact is data/reference_parameters.md (our
# transcription of the parameter tables), not the PDFs themselves.
Reference/

# Default-deny on PDFs so a paper dropped anywhere in the tree cannot be
# committed by accident.
*.pdf

# Manuscript kept out of version control: the public archive is code and data.
# The two documents in there that belong WITH the code -- the model equations
# and the parameter provenance record -- live in docs/ instead.
投稿PLOS Comp Biol/
投稿Biomechanics and Modeling in Mechanobiology/
